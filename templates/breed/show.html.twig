{% extends 'base.html.twig' %}

{% block title %}Les races{% endblock %}

{% block body %}
    <h1>Les races</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ breed.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ breed.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_breed_index') }}">Retour à la liste</a>

    <a href="{{ path('app_breed_edit', {'id': breed.id}) }}">Modifier</a>

    {{ include('breed/_delete_form.html.twig') }}
{% endblock %}
